platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: 1.17.9-bullseye

inputs:
- name: external-secrets-trunk

outputs:
- name: external-secrets-trunk

run:
  path: bash
  args:
  - -c
  - |
    set -euo pipefail
    cd external-secrets-trunk
    go install github.com/mikefarah/yq/v4@latest
    make build-amd64