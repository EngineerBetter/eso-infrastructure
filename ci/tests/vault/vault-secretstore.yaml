apiVersion: v1
kind: Secret
metadata:
  name: vault-token
stringData:
  token: hvs.SymSDxhHURawL6hfBjUyOaAQ
---
apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: vault-backend
spec:
  provider:
    vault:
      server: "http://10.96.109.220:8200"
      path: "secret" # change to the right values
      version: "v2" # change to the right values
      auth:
        # points to a secret that contains a vault token
        # https://www.vaultproject.io/docs/auth/token
        tokenSecretRef:
          name: "vault-token"
          key: "token"

