apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: vault-external-secret
  namespace: default
spec:
  refreshInterval: 1m
  secretStoreRef:
    name: vault-secretstore
    kind: SecretStore
  data:
    - secretKey: new-key
      remoteRef:
        key: target-foobar
