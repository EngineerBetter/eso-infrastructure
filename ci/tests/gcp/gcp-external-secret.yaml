apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: gcp-external-secret
  namespace: default
spec:
  refreshInterval: 1m
  secretStoreRef:
    name: gcp-secretstore
    kind: SecretStore
  data:
    - secretKey: new-key
      remoteRef:
        key: foo-bar
