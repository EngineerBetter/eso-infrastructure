apiVersion: external-secrets.io/v1alpha1
kind: ExternalSecret
metadata:
  name: external-secret-example
  namespace: default
spec:
  refreshInterval: 1m
  secretStoreRef:
    name: secretstore-example
    kind: SecretStore
  data:
    - secretKey: new-key
      remoteRef:
        key: /foo/bar
