jobs:

- name: hello-world
  plan:
  - task: simple-task
    config:
      platform: linux
      image_resource:
        type: registry-image
        source: { repository: busybox}
      run:
        path: echo
        args: ["Hello world! My credential example_cred: ((example_cred))"]

